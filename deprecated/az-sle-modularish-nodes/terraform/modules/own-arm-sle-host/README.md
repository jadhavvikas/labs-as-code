## Creates a SLE Instance

### Provides

* Public IP
* Virtual Nic
* Virtual Machine
    * Optional additional blobs

### Usage

```{text}
```

### Notes

* Inherits `location` and `tags` from passed `resource_group_name`.
