## Creates a useable default nsg for cloud hosts

### Provides

* `nsg` that allows port 22 fron everywhere

### Usage

```{text}
```

### Notes

* Inherits `location` and `tags` from passed `resource_group_name`.
